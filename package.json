{
  "name": "crossword",
  "dependencies": {
    "elm": "^0.19.0",
    "elm-format": "^0.8.1"
  },
  "scripts": {
    "reactor": "./node_modules/.bin/elm reactor",
    "format": "./node_modules/.bin/elm-format src/ --yes",
    "build": "elm make --optimize --output=build/elm.js src/Main.elm && cp src/index.html build/"
  }
}
